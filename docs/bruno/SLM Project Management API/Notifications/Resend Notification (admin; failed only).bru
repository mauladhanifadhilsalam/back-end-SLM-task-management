meta {
  name: Resend Notification (admin; failed only)
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/notifications/:id/resend
  body: json
  auth: inherit
}

params:path {
  id: 
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "subject": "Retry: ticket assignment",
    "text": "Re-sending assignment email.",
    "from": "noreply@slm.local",
    "replyTo": "support@slm.local"
  }
}

settings {
  encodeUrl: true
}
