meta {
  name: Notifications
  seq: 12
}

auth {
  mode: inherit
}

docs {
  Non-admins only see their own notifications. Admin-only: create/update/resend/delete. Recipients can mark their own notifications READ.
}
