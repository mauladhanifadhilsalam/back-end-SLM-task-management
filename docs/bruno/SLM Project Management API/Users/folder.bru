meta {
  name: Users
  seq: 3
}

auth {
  mode: inherit
}

docs {
  User management. `GET /users` requires ADMIN or PROJECT_MANAGER; CRUD is ADMIN-only; change-password is open to any authenticated user.
}
